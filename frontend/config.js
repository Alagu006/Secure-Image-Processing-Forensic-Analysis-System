// Replace this URL with your actual Render deployment URL after deploying.
// https://dashboard.render.com → your service → copy the ".onrender.com" URL
window.API_BASE = window.API_BASE || "http://localhost:8000";
// Set this if you enable the API_KEY env var on the server
window.API_KEY = "";
